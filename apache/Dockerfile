ARG APACHE_VERSION

FROM httpd:${APACHE_VERSION}

ARG APACHE_CONFIG_TARGET
ARG APACHE_CONFIG_SOURCE
ARG APACHE_CONFIG_INCLUDE

RUN apk update; \
    apk upgrade; \
    apk add bash;

COPY $APACHE_CONFIG_SOURCE $APACHE_CONFIG_TARGET

RUN echo "Include $APACHE_CONFIG_TARGET" >> $APACHE_CONFIG_INCLUDE